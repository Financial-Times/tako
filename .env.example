# The ID of your GitHub App
APP_ID=

WEBHOOK_SECRET=development

# Use `trace` to get verbose logging or `info` to show less
LOG_LEVEL=info

# Go to https://smee.io/new set this to the URL that you are redirected to.
WEBHOOK_PROXY_URL=

# The value for the Authorization header to secure the repositories endpoint. e.g. `curl -H 'Authorization: Bearer hunter2' http://localhost:3000/tako/repositories`
# BEARER_TOKEN=

# No need for the built-in stats endpoint.
DISABLE_STATS=true
